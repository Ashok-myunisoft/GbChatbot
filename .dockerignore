# Secrets — never bake into the image
.env

# Python cache
__pycache__/
*.py[cod]
*.pyo
*.pyd

# Virtual environments
.venv/
venv/

# Local vectorstore data (mount via volume instead)
memory_vectorstore_formula/
memory_vectorstore_report/
conversational_memory_vectorstore/
menu_memory_vectorstore/

# Data files used to build the general RAG knowledge base
# Keep the runtime volume mount in docker-compose.yml so local changes still work.
# The image now includes these docs as a safe fallback when the mount is absent.

# Dev / editor artefacts
.git/
.gitignore
*.md
.DS_Store
.idea/
.vscode/

# Docker files (no need to copy into image)
Dockerfile
docker-compose.yml

# Dev / utility scripts (not needed at runtime)


# Temp / log files
*.log
*.tmp
